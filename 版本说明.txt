版本：1.1

主要功能：
- Windows Tkinter 图形界面
- 直接选择原始 .docx / .txt 和输出目录
- API Key 密文输入且不保存
- 可测试 API、刷新模型列表
- 默认关闭深度思考以节省费用
- 自动读取完整单词本整理规则
- Kimi JSON Mode 结构化输出
- 自动生成三列表格 Word 单词本
- 保留原始 JSON 和解析后 JSON 便于核查
- 一键打包为无黑色终端窗口的单文件 EXE
